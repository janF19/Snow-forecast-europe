
# Node modules
node_modules/

# Isolated implementation worktrees
.worktrees/

# Build folders
dist/
build/

weather_responseTrash
python_help_scripts


# Environment variables
.env

# System files
.DS_Store

# Python cache
.cache
.cache.sqlite
__pycache__/
*.pyc

# Large OpenSkiMap snapshot -- fetched at run time, never committed.
# Only the manifest (freeride/data/openskimap/manifest.json) is tracked.
freeride/data/openskimap/*.geojson
freeride/data/dem/

# Forecast snapshots accumulate daily; keep only the README.
data/forecast_snapshots/*.jsonl
